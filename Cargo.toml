[package]
name = "zerovisor"
version = "0.1.0"
edition = "2021"
authors = ["Zerovisor Team"]
description = "World-class Type-1 hypervisor written in Rust"

[lib]
name = "zerovisor"
path = "src/lib.rs"

[dependencies]
zerovisor-core = { path = "zerovisor-core" }
zerovisor-hal = { path = "zerovisor-hal" }

# Architecture-specific optional dependencies
zerovisor-x86_64 = { path = "zerovisor-x86_64", optional = true }
zerovisor-arm64 = { path = "zerovisor-arm64", optional = true }
zerovisor-riscv = { path = "zerovisor-riscv", optional = true }
sha2 = { version = "0.10", default-features = false }

[features]
default = []
x86_64 = ["zerovisor-hal/x86_64", "zerovisor-x86_64"]
arm64 = ["zerovisor-hal/arm64", "zerovisor-arm64"]
riscv = ["zerovisor-hal/riscv", "zerovisor-riscv"]

[workspace]
members = [
    "zerovisor-core",
    "zerovisor-hal",
    "zerovisor-x86_64",
    "zerovisor-arm64", 
    "zerovisor-riscv",
    "zerovisor-bootloader"
]

[workspace.dependencies]
# Core dependencies for no_std environment
spin = "0.9"
bitflags = "2.4"
static_assertions = "1.1"
sha2 = { version = "0.10", default-features = false }

# Architecture-specific dependencies
x86_64 = "0.14"
cortex-a = "8.1"
riscv = "0.10"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = "z"